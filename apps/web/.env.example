# Marketing landing site configuration
#
# URL of the dashboard app (apps/dashboard), served on the app.* subdomain.
# Used for the header "Log in" link and the "Deploy node" CTAs, which are
# cross-origin links from this landing site into the authenticated app.
NEXT_PUBLIC_APP_URL=https://app.maronnodes.xyz
